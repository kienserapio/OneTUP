node_modules/
.pnpm-store/

.next/
out/
dist/
build/
*.tsbuildinfo

.env
.env.*
!.env.example

coverage/
.vitest/
playwright-report/
test-results/

.DS_Store
*.log
.vercel
.turbo
.playwright-mcp/
AGENTS.md
CLAUDE.md

# Playwright MCP scratch output
.playwright-mcp/

# Stray screenshots dropped at the repo root. Anything meant for the README
# belongs in docs/assets/ and is committed there deliberately.
/*.png
/*.jpg

# Real documents scanned during development. A rating slip or a grade report
# is exactly the data this project promises never to expose — it does not go
# into a public repository, not even as a fixture.
docs/rating-slip.pdf
docs/*-slip.pdf
.env*
